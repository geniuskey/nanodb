# 요구사항 확인 질문

> 아래 질문과 답변은 최초 8시간 범위에서 확정한 이력이다. 2026-09-08 요구사항은 5명·3일 독립 개발로 변경되었으며, 기존 확장 비활성화 선택 B/B/C는 유지한다. 현재 일정은 MVP 요구사항 9절을 따른다.

홈 탭 요구사항 자체는 PDF와 기존 8시간 MVP 제약으로 충분히 명확합니다. 아래 항목은 저장소에 설치된 AI-DLC 확장의 필수 선택 사항입니다. 각 `[Answer]:` 뒤에 선택 문자를 입력해 주세요.

## Question 1: 복원력 확장

이 프로젝트에 복원력 기준을 적용할까요?

A) 예 — 고가용성, 관측성, 복구 가능성에 관한 방향성 설계 지침을 적용합니다.

B) 아니요 — 8시간 PoC의 빠른 구현을 위해 건너뜁니다.

X) 기타 — `[Answer]:` 뒤에 원하는 범위를 설명해 주세요.

[Answer]: B

## Question 2: 보안 확장

이 프로젝트에 보안 확장 규칙을 필수 제약으로 적용할까요?

A) 예 — 프로덕션 수준을 목표로 모든 보안 규칙을 필수 적용합니다.

B) 아니요 — 인증과 운영 보안을 제외한 8시간 PoC이므로 건너뜁니다.

X) 기타 — `[Answer]:` 뒤에 원하는 범위를 설명해 주세요.

[Answer]: B

## Question 3: 속성 기반 테스트 확장

속성 기반 테스트(Property-Based Testing) 규칙을 적용할까요?

A) 예 — 비즈니스 로직, 데이터 변환, 직렬화, 상태 처리 전반에 적용합니다.

B) 부분 적용 — 순수 함수와 직렬화 왕복 테스트에만 적용합니다.

C) 아니요 — 단순 CRUD와 UI 중심의 8시간 MVP이므로 건너뜁니다.

X) 기타 — `[Answer]:` 뒤에 원하는 범위를 설명해 주세요.

[Answer]: C

---

# 2026-09-08 UI/UX 완성도 요구사항 확인 질문

> 배경: [ui-ux-review-2026-09-08.md](ui-ux-review-2026-09-08.md) 6절 R2 묶음.
> 아래 7개 질문만 사용자 결정이 필요합니다. R1(정합화)·R3(범위 선언)·R4(공통 UX)는 답변과 무관하게 반영합니다.
> 각 `[Answer]:` 뒤에 선택 문자를 적어 주세요. **전부 추천안대로 진행해도 되면 Question 1의 `[Answer]:`에 `추천`이라고만 적어 주세요.**

## Question 1: 도형 라벨링(화살표·원)의 요구사항 지위

화살표·원 도형과 도형별 `제품/Step/측정 항목 명` 입력이 구현돼 있으나 요구사항 ID가 없고, 현재 제외 범위의 "윤곽 라벨링"과 충돌합니다. 어떻게 정리할까요?

A) **P1 정식 기능으로 승격 (추천)** — `ANN-001~008`을 신설하고 제외 범위의 "윤곽 라벨링"을 "자유 폴리곤 라벨링과 라벨 검수·버전"으로 좁혀 구현된 2종 도형과 구분합니다. 도형 삭제 기능을 추가합니다.

B) P0 필수 기능으로 승격 — 데모 게이트에 포함하고 수용 기준·e2e 테스트까지 P0로 올립니다.

C) 실험 기능으로 격리 — 요구사항에 "미검증 실험 기능"으로만 기록하고 홈·발표 자료에서는 기능으로 소개하지 않습니다.

D) 제거 — 요구사항과 맞추기 위해 코드·테이블·API를 되돌립니다.

X) 기타 (please describe after [Answer]: tag below)

[Answer]: 

## Question 2: 도형 데이터를 개발 컨텍스트 ZIP에 포함할지

현재 도형과 도형 라벨은 `context.md`/`data.json`에 들어가지 않습니다. 제품 명제상 라벨링 데이터만 AI 개발 컨텍스트에서 빠져 있는 상태입니다.

A) **포함하되 계약 버전을 올린다 (추천)** — `data.json`에 `annotations[]`를 추가하고 `schema_version`을 갱신, `context.md`에 좌표계·의미·"수동 미검토 참고" 상태를 명시합니다. CTX-012 재현성 검증도 확장합니다.

B) 포함하지 않는다 — CTX 계약을 P0 게이트 통과 상태 그대로 동결하고, 도형은 앱 내부 표시용으로만 둔다고 요구사항에 명시합니다.

C) 별도 파일로 포함 — `annotations.json`을 ZIP에 추가하되 기존 4개 파일 계약은 건드리지 않습니다.

X) 기타 (please describe after [Answer]: tag below)

[Answer]: 

## Question 3: 홈 소개 영상(YouTube 자동재생)

외부 런타임 의존 금지(HOM-026)·로컬 실행 전제와 충돌하며, 네트워크가 막힌 심사 환경에서는 홈 최상단이 빈 프레임이 됩니다.

A) **유지하되 안전장치를 요구사항화 (추천)** — 로드 실패 시 대체 이미지·설명 텍스트를 표시하고, `prefers-reduced-motion`에서는 자동 재생하지 않으며, 오프라인 대체 경로를 README에 기록하도록 `HOM-040`을 신설합니다.

B) 클릭 재생으로 변경 — 자동 재생을 없애고 포스터 이미지 + 재생 버튼으로 바꿉니다. 나머지는 A와 동일합니다.

C) 홈 하단으로 이동 — 첫인상은 히어로가 담당하고 영상은 보조 자료로 내립니다.

D) 제거 — 홈에서 영상을 빼고 평가용 VitePress 사이트에만 링크로 둡니다.

X) 기타 (please describe after [Answer]: tag below)

[Answer]: 

## Question 4: 홈 CTA — 어느 문서가 정본인가

HOM-005/023은 "홈 본문에 이동 링크 없음"이고, MVP 문서 5.2절과 통합 요구사항 4.2절은 "CTA 필수"입니다. 구현은 CTA가 없습니다.

A) **HOM v2를 정본으로 하되 사용 흐름 섹션에만 CTA 1쌍 허용 (추천)** — `이렇게 쓰세요` 4단계 끝에 `이미지 등록`·`이미지 둘러보기` 두 개만 두고, 나머지 본문은 링크 없는 읽기 문서로 유지합니다. 상위 문서 2종을 이에 맞게 수정합니다. 사용성 관찰 결과 리스크를 줄이면서 v2의 읽기 흐름을 지킵니다.

B) HOM v2를 그대로 정본으로 — CTA를 완전히 두지 않고, 상위 문서 2종의 CTA 문장과 수용 기준을 삭제합니다. 이동은 상단 탭바만 담당합니다.

C) 상위 문서를 정본으로 — 히어로에 primary/secondary CTA를 되살리고 HOM-005/023을 폐기합니다.

X) 기타 (please describe after [Answer]: tag below)

[Answer]: 

## Question 5: 정밀 측정을 위한 확대·이동

원본 4096px 이미지가 1000px로 렌더되면 화면 1px = 원본 4px이라, 요구된 1px 정확도를 사용자가 지정할 수 없습니다.

A) **배율 표시 + 확대·이동 제공 (추천)** — 확대·이동을 `MEA-013`으로 추가하고, 현재 배율에서 화면 1px이 원본 몇 px인지 `MEA-014`로 표시합니다. 좌표는 언제나 원본 기준으로 계산합니다.

B) 배율 표시만 — 확대는 넣지 않고, 현재 배율과 지정 가능한 정확도만 표시해 한계를 정직하게 알립니다.

C) 현행 유지 + 제외 범위 명시 — 확대·이동을 제외 범위로 선언하고, 정확도 요구를 "복원 정확도"로만 한정한다고 문서에 못 박습니다.

X) 기타 (please describe after [Answer]: tag below)

[Answer]: 

## Question 6: 저장된 측정의 수정

현재는 저장 후 좌표·항목·메모를 고칠 수 없고 삭제 후 재측정만 가능합니다. 제외 범위에도 언급이 없습니다.

A) **메모만 수정 가능 (추천)** — 측정 근거(좌표·항목·값)는 불변으로 유지해 감사 가능성을 지키고, 메모 수정만 허용합니다. 좌표·항목 수정과 수정 이력은 제외 범위에 명시합니다.

B) 전체 수정 허용 — 좌표·항목까지 수정하고 `updated_at`을 남깁니다.

C) 수정 없음 — 현행 유지하되 "저장 측정의 수정과 수정 이력"을 제외 범위에 명시만 합니다.

X) 기타 (please describe after [Answer]: tag below)

[Answer]: 

## Question 7: 측정 상호작용의 키보드 접근

측정은 이미지 클릭 전용, 도형은 포인터 드래그 전용이라 키보드만으로는 측정을 시작할 수 없습니다.

A) **좌표 직접 입력 대안 제공 (추천)** — 시작점·끝점 X/Y를 수치로 입력해 측정을 만들 수 있게 하고, 이를 `UIX-003`으로 요구사항화합니다. 정밀 지정 수단(Question 5)과도 겹쳐 이득이 큽니다.

B) 방향키 커서 이동 — 이미지를 포커스 가능하게 만들고 방향키로 점을 이동, Enter로 확정합니다.

C) 제외 범위 명시 — "포인터 장치 없는 측정"을 제외 범위로 선언하고 미구현임을 정직하게 남깁니다.

X) 기타 (please describe after [Answer]: tag below)

[Answer]: 
